import React from 'react';
import './styles.scss';
import PropTypes from 'prop-types';

const OK = () => {
  const wow = 'truc';
  return (
    <div>Ok</div>
  );
};

OK.propTypes = {};

export default OK;
